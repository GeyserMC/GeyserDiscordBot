type: text
aliases: log, mclogs

---

If you want to share your logs while keeping your IP addresses private, use https://mclo.gs/ and give us the link after you saved it. 
For bungee there is a `proxy.log.0` file in the root and for everything else it is `latest.log` in `logs` folder.