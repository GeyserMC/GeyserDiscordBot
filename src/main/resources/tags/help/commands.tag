type: text
aliases: tp, op
title: :tools: Commands with Bedrock players
color: help

---

If you are having issues with using commands such as `/tp` with Bedrock players while using Floodgate; you could try using the format below. Setting the floodgate username prefix to `.` (which is the default) also solves this issue.
`/tp ".BedrockPlayer"`
