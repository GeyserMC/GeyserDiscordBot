type: text

---

If you own the server, you can set `moved-wrongly-threshold` in your `spigot.yml` to the value 1.0075 to partially fix Bedrock movement at the expense of making it slightly easier to movement hack.
